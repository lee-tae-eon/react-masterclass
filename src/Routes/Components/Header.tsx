import styled from "styled-components";

const Nav = styled.nav``;

const Col = styled.div``;

function Header() {
  return (
    <Nav>
      <Col></Col>
      <Col></Col>
      <Col></Col>
    </Nav>
  );
}

export default Header;
